
{% extends 'base.html.twig' %}

{% block title %}Liste des Burgers{% endblock %}

{% block body %}
    <h1>Nos Burgers 🍔</h1>

    <ul>
        <li><a href="{{ path('burger_show', { id: 1 }) }}">Cheeseburger</a></li>
        <li><a href="{{ path('burger_show', { id: 2 }) }}">Bacon Burger</a></li>
        <li><a href="{{ path('burger_show', { id: 3 }) }}">Vegan Burger</a></li>
    </ul>

{% endblock %}